<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android_Onboarding_Project"
        tools:targetApi="31"
        >
        <activity
            android:name=".ShowAllUsersActivity"
            android:exported="false"
            android:label="All Users Data">
        </activity>
        <activity
            android:name=".RegisterPageActivity"
            android:label="Registration Portal">
        </activity>
        <activity
            android:name=".LoginWelcomeActivity"
            android:label="Welcome">
        </activity>
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Login Portal"
            android:screenOrientation="fullSensor"
            android:configChanges="orientation"/>
    </application>

</manifest>